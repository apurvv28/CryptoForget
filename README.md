# CryptoForget
**CryptoForget** — Verifiable machine unlearning on a live MLOps pipeline. Simulated users interact with a LightGBM risk model; a "Delete My Data" control triggers SISA-based unlearning and issues a cryptographically signed deletion certificate (Merkle proofs + ECDSA),  provable removal without exposing model weights, MIA-verified.
